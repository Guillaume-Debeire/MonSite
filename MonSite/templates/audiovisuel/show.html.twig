{% extends 'base.html.twig' %}

{% block title %}Audiovisuel{% endblock %}

{% block body %}
        <div class="show_page">
        <header class="show_header">
            <a class="button_back" href="{{ path('production') }}">Retour à la liste</a>
            <div class="show_header_title">
                <a class="nav_button" href="#">&#8249;</a>
                <div>
                    <h1>{{ audiovisuel.name }}</h1>
                </div>
                
                <a class="nav_button" href="#">&#8250;</a>
            </div>
            <h4>{{ audiovisuel.type }}</h4>
            <h5>{{ audiovisuel.dateDebut }} -> {{ audiovisuel.dateFin }}</h5>
        </header>
        <div class="show_body">
            <div class="show_center_audiovisuel">
                <iframe class="youtube" width="560" height="315" src="{{ audiovisuel.link }}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
        </div>
    </div>



    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ audiovisuel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ audiovisuel.name }}</td>
            </tr>
        </tbody>
    </table> #}


    {{ include('audiovisuel/_delete_form.html.twig') }}
{% endblock %}
