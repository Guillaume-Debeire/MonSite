{% extends 'base.html.twig' %}

{% block title %}Parcours{% endblock %}

{% block body %}
    <header class="show_header">
        <h1>{{ parcour.name }}</h1>
        <h3>Formation développeur web & web mobile  </h3>
    </header>
    <body>
        <div>
            <p>{{ parcour.description }}</p>
        </div>
        <div class="software_index">
            
        </div>
        <div class="parcour_photo">
            {# {{ for photo in photos }}
                
            {{ endfor }} #}
        </div>
        {% if (parcour.YoutubeVideo is defined) %}
            <div>
                <iframe class="youtube" width="560" height="315" src="{{ parcour.YoutubeVideo }}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
        {% endif %}
    </body>

    <a href="{{ path('parcours') }}">back to list</a>

    <a href="{{ path('parcours_edit', {'id': parcour.id}) }}">edit</a>

    {{ include('parcours/_delete_form.html.twig') }}
{% endblock %}
